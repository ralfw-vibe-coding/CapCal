- im kalender das bearbeitungsfenster soll alle angaben zeigen. dann kann der kalendereintrag reduziert werden auf das wesentliche. das gilt dann auch für die allokationen.

- tagesvorlagen: einen tag mit seinen buchungen als vorlage speichern können. vorlage auf einen tag anwenden können.
vorlage löschen. sollen buchungen in vorlagen autom. zu aufgaben werden? aufgaben nur im tree anlegen können?

- API key: upload/download als API endpunkte veröffentlichen. versionierung kann bei upload erzwungen werden mit optionalem parameter. bei upload: schema verifizieren und konsistenz prüfen. welche konsistenzregeln gibt es? beispiel: es können nur IDs verwendet werden, zu denen ein datensätze gibt. uhrzeiten müssen gültig sein. was noch?